import sys
input = sys.stdin.readline

qu = int(input())
waterlist = [0] * (qu + 1)
daylist = [0] * (qu + 1)
for i in range(qu):
    a, b = map(int, input().split())
    timelist[i + 1] = max(timelist[i])

